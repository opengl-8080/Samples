apply plugin: 'java'

sourceCompatibility = '1.8'
targetCompatibility = '1.8'

compileJava.options.encoding = 'UTF-8'

jar {
    manifest {
        attributes("Main-Class": "Main")
    }
}

task run(dependsOn: jar, type: Exec) {
    commandLine "java", "-jar", jar.archivePath
}