task foo {
    doFirst {
        project.sync {
            from "fromDir"
            into "toDir"
            include "**/*.txt"
            preserve {
                include "hoge.txt"
            }
        }
    }
}
