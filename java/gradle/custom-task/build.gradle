plugins {
    id "base"
}

task hoge {
    def outDir = file("${buildDir}/hoge")
    outputs.dir outDir

    doFirst {
        mkdir outDir
        file("${outDir}/hoge.txt") << "Hoge!!"
    }
}